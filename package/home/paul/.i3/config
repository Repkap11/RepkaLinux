#This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like
#
# Edited by Paul Repka for Ubuntu 12.04
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $ALT Mod1

# foemot for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted positionn
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
bindsym Ctrl+$ALT+t exec i3-sensible-terminal

# stat file manager
bindsym Ctrl+Mod1+N exec i3

# kill focused window
bindsym $mod+Shift+Q kill
bindsym $ALT+F4 kill
bindsym $mod+F4 kill

# Shutdown Computer
#bindsym $mod+Shift+S exec gksudo "shutdown -h now"
bindsym $mod+Delete exec dbus-send --system --print-reply --dest="org.freedesktop.ConsoleKit" /org/freedesktop/ConsoleKit/Manager org.freedesktop.ConsoleKit.Manager.Stop
bindsym $mod+Insert exec php ~/Software/PHP-LG-SmartTV/example.php 1
#bindsym Ctrl+Mod1+Delete exec i3-exit

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
bindsym Ctrl+Mod1+d exec dmenu_run
bindsym Ctrl+Mod1+x exec sh ~/.screenlayout/768p.sh
bindsym $mod+x exec sh ~/.screenlayout/default.sh
bindsym Ctrl+Mod1+4 kill

# change focus
#bindsym $mod+j focus left
#bindsym $mod+k focus down
#bindsym $mod+l focus up
#bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
#bindsym $mod+Left focus left
#bindsym $mod+Down focus down
#bindsym $mod+Left move container to workspace 1
#bindsym $mod+Right move container to workspace 2
#bindsym $mod+Left move container to workspace 1
#bindsym $mod+Right move container to workspace 2

bindsym $mod+Up focus up
bindsym $mod+Right focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down



# move focused window
#bindsym $mod+Shift+J move left
#bindsym $mod+Shift+K move down
#bindsym $mod+Shift+L move up
#bindsym $mod+Shift+colon move right


# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
#bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+s layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+exclam move container to workspace 1
bindsym $mod+Shift+at move container to workspace 2
bindsym $mod+Shift+numbersign move container to workspace 3
bindsym $mod+Shift+dollar move container to workspace 4
bindsym $mod+Shift+percent move container to workspace 5
bindsym $mod+Shift+asciicircum move container to workspace 6
bindsym $mod+Shift+ampersand move container to workspace 7
bindsym $mod+Shift+asterisk move container to workspace 8
bindsym $mod+Shift+parenleft move container to workspace 9
bindsym $mod+Shift+parenright move container to workspace 10



# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R exec i3-msg restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+E exit

# Add screenshot support
bindsym Print exec gnome-screenshot

# Add alt-tab
bindsym $mod+tab focus left

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+u mode "resize"

# Custom Window Config?
new_window none
for_window [class="Gtk-recordmydesktop"] floating enable border none
for_window [instance="file_progress"] floating enable border normal
for_window [class="net-minecraft-launchwrapper-Launch"] floating enable border none
#for_window [class="vlc"] floating enable border normal
for_window [class="VirtualBox"] floating enable border normal
for_window [class="I3-exit"] floating enable border normal
for_window [class="conky"] floating enable border none
for_window [class="InputOutput"] floating enable border normal
#for_window [class="Eclipse" title="Eclipse"] floating enable border normal
for_window [class="net-ftb-gui-LaunchFrame"] floating enable border normal
for_window [class="Xfce4-notifyd"] floating enable border none
for_window [class="classname-of-google-chrome"] border none
#for_window [class="jagexappletviewer"] border none
for_window [window_role="pop-up"] floating enable border normal

for_window [class="Runescape"] move container to workspace 10
for_window [class="RuneScape"] move container to workspace 1


# float windows borders
new_float normal

# Color settings
set $blue #005588
# white
set $gray0 #FFFFFF
set $gray1 #CCCCCC
set $gray2 #666666
set $gray3 #555555
set $gray4 #333333
set $gray5 #000000
#black

#			  outside  inside   text
client.focused          $gray3   $gray2   $gray0   #000000
#client.focused_inactive   $gray4   $gray2   $gray1    #000000
client.unfocused            $gray3   $gray4   $gray1   #000000


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status --config /home/paul/.i3/i3status.conf
	tray_output eDP-1-1
	workspace_buttons yes
	#font xft:DejaVu Sans Mono 10
	#font xft:DejaVu Sans Mono 10
	#font xft:Ubuntu Sans Mono 12
	font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
	colors {
	        background #000000
	        statusline #ffffff
		# outside inside text
                focused_workspace $gray3 $gray4 $gray0
                inactive_workspace $gray4 $gray5 $gray2

	        active_workspace   #ffffff #333333
	        urgent_workspace   #ffffff #900000
    	}	
}

# Enable flux
#exec redshift -l 43:-77

# Start ownCloud on login
#exec owncloud

# Start the unity-settings-daemon to fix fonts, audio, brightness
exec --no-startup-id unity-settings-daemon

# Set the background
exec_always --no-startup-id feh --bg-fill /home/paul/.i3/background.jpg

# Set it again so it looks nice after loading in a 2nd monitor
exec_always --no-startup-id sleep 5 && feh --bg-fill /home/paul/.i3/background.jpg


# Starts up the NetworkManager applet
exec --no-startup-id nm-applet

# Controls the password management for applications, network managers
exec --no-startup-id gnome-keyring-daemon

# Starts up Conky
#exec sleep 10 && conky -c ~/.conky/conkyrc_grey

# volume control
#bindsym XF86AudioRaiseVolume exec z
#bindsym XF86AudioLowerVolume exec amixer -q set Master 2dB- unmute
#bindsym XF86AudioMute exec amixer -q set Master toggle

bindsym XF86AudioLowerVolume exec "amixer -D pulse sset Master 5%- ; killall -USR1 i3status"
bindsym XF86AudioRaiseVolume exec "amixer -D pulse sset Master 5%+ ; killall -USR1 i3status"
#bindsym XF86AudioPlay exec playerctl play
#bindsym XF86AudioPause exec playerctl pause
#bindsym XF86AudioNext exec playerctl next
#bindsym XF86AudioPrev exec playerctl previous

# brightness control
bindsym XF86MonBrightnessDown exec xbacklight -steps 1 -time 0 -dec 5
bindsym XF86MonBrightnessUp exec xbacklight -steps 1 -time 0 -inc 5
bindsym XF86AudioMute exec /usr/bin/pactl set-sink-mute @DEFAULT_SINK@ toggle

# show battery stats
bindsym XF86Battery exec gnome-power-statistics




exec_always sleep 1 && ~/.screenlayout/hdmi-right.sh
exec_always ~/.screenlayout/hdmi-right.sh
exec sleep 1 && xset m 0.9
#exec sleeep1 && xinput --set-prop 13 "libinput Accel Speed" 0

bindsym $mod+i exec gedit /home/paul/.i3/config
bindsym $mod+b exec i3bar-toggle
bindsym $mod+l exec /usr/bin/i3lock_gaus

#for_window [title="^RepGame$"] floating enable border normal
#assign [class="LibGDX_Desktop"] 2

bindsym $mod+r exec "runescape-launcher & wmctrl -r runescape -b remove,demands_attention ; sleep 3 ; wmctrl -r runescape -b remove,demands_attention"
bindsym $mod+m exec minecraft
bindsym $mod+t exec telegram
bindsym $mod+n exec thunar
bindsym $mod+g exec google-chrome
bindsym $mod+Shift+u exec i3-get-window-criteria | xclip -selection c

exec sleep 3 && xkbset -sl # turns off Slow-Keys
exec sleep 3 && xkbset -a  # turns off Acessability Features so Slow-Keys won't turn on again

exec sleep 3 && gsettings set org.gnome.desktop.a11y.keyboard enable false
exec sleep 3 && synclient TouchPadOff=1

# Turn off Mousekeys
exec sleep 3 && gsettings set org.gnome.desktop.a11y.keyboard mousekeys-enable false

# Change nautilus settings to not show the desktop
exec gsettings set org.gnome.desktop.background show-desktop-icons false
exec gsettings set org.gnome.desktop.background draw-background false

#Make the MSI slash key into a control modifier
exec_always xmodmap slash-to-control

hide_edge_borders both
bindsym $mod+Shift+f exec factorio
bindsym $mod+Shift+s exec i3-msg workspace 1 && i3-msg workspace 3 && i3-msg workspace 2 && i3-msg workspace 1 && i3-msg workspace 3 && i3-msg workspace 2
#exec dropbox start
#exec caffeine
#bindsym KEY_LEFTMETA exec notify-send Test Left Meta
# exec sleep 1 && i3-msg workspace 1; exec i3-msg exec runescape -h
#exec sleep 2 && i3-msg workspace 2; exec i3-msg exec google-chrome
bindsym $mod+Ctrl+Left move workspace to output left
bindsym $mod+Ctrl+Right move workspace to output right
bindsym $mod+c exec compton-toggle
exec xbindkeys
exec slee 5 && telegram -startintray
exec --no-startup-id compton-start
exec i3-msg workspace 2
exec i3-msg layout tabbed
exec i3-msg workspace 1
exec i3-msg layout tabbed
exec x11vnc -forever -rfbauth ~/.vnc/passwd
#exec ssh -f -N -T -R 0.0.0.0:13562:localhost:5900 root@repkap11.com
exec link-temp-device
#exec sleep 5 && runescape -h
exec google-chrome
